<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cai.work">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name="com.cai.work.base.App"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:name">
        <activity
            android:name=".ui.welcome.WelcomeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.main.MainActivity" />
        <activity android:name=".ui.web.WebActivity" />
        <activity android:name=".ui.candy.CandyDetailActivity" />
        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.about.AboutActivity" />
        <activity android:name=".ui.message.MessageActivity" />
        <activity android:name=".ui.person.PersonActivity" />
        <activity android:name=".selectimg.ISCameraActivity" />
        <activity android:name=".selectimg.ISListActivity" />
        <activity android:name=".ui.nationcode.NationCodeActivity" />
        <activity android:name=".ui.record.RecordActivity" />

        <meta-data
            android:name="UMENG_APPKEY"
            android:value="5b20a535a40fa3053200023a" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="MORE.ONE" />
        <!-- 配置APP ID -->
        <meta-data
            android:name="BUGLY_APPID"
            android:value="7f5cfd9b86" />
        <!-- 配置APP版本号 -->
        <meta-data
            android:name="BUGLY_APP_VERSION"
            android:value="new_2.1.3.8" />
        <!-- 配置APP渠道号 -->
        <meta-data
            android:name="BUGLY_APP_CHANNEL"
            android:value="1" />
        <!-- 配置Bugly调试模式（true或者false）-->
        <meta-data
            android:name="BUGLY_ENABLE_DEBUG"
            android:value="false" />
    </application>

</manifest>